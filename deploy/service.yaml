apiVersion: v1
kind: Service
metadata:
  name: homelab-dashboard
  namespace: homelab-dashboard
spec:
  type: NodePort
  selector:
    app: homelab-dashboard
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30095
